@inherits LayoutComponentBase

<div class="d-flex">
    <!-- Sidebar -->
    <div class="sidebar bg-dark text-white vh-100 p-3" style="width: 220px;">
        <h4 class="text-white mb-4">Dastkari Admin</h4>

        <NavLink href="sales" class="nav-link custom-link mb-2" activeclassname="active">
            <i class="bi bi-bar-chart-fill me-2"></i> Analytics
        </NavLink>

        <NavLink href="counter" class="nav-link custom-link mb-2" activeclassname="active">
            <i class="bi bi-box-seam me-2"></i> Product Management
        </NavLink>

        <NavLink href="inventoryadmin" class="nav-link custom-link mb-2" activeclassname="active">
            <i class="bi bi-speedometer2 me-2"></i>Artisan Applications
        </NavLink>

        <NavLink href="/" class="nav-link custom-link mb-2" activeclassname="active">
            <i class="bi bi-house-door-fill me-2"></i> Back to Home
        </NavLink>
    </div>

    <!-- Main Content -->
    <div class="flex-grow-1 p-1" style="margin-left: 10px;">
        <main>
            <article class="content">
                @Body
            </article>
        </main>
    </div>
</div>

        <style>
    .custom-link {
        color: white;
        padding: 8px 12px;
        border-radius: 8px;
        transition: all 0.2s ease-in-out;
    }

        .custom-link:hover {
            background-color: rgba(255, 255, 255, 0.1);
            padding-left: 18px;
            text-decoration: none;
        }

        .custom-link.active {
            background-color: #CC5500;
            color: black !important;
            font-weight: 600;
        }

</style>